interface:
  display_name: "/xxd-panel-020"
  short_description: "Create Panel 020 from its original style brief."
  default_prompt: "Use $xxd-panel-020; keep the body of its bundled original style brief as the sole aesthetic authority, let the image model compose each complete canvas without fixed panel boxes unless I request exact geometry, and keep prompt-generated wording rooted in the current source image."
