# Source-note: XXD Panel 020 tweet body

The X post identified as `VOL.020` is titled “GPT2 × 厚涂 × 立体感”, and its four attached samples consistently show an impasto miniature-scene aesthetic. However, the prompt body published in that post is byte-for-byte identical to the local `VOL.015` geometric-abstraction prompt after the administrative heading is removed.

That duplicated body is retained in Git history. It is not used at runtime because it contradicts the title and all four original `VOL.020` sample images. The active `zh-CN.md` is a transparent reconstruction of the demonstrated 020 style, created to prevent Panel 020 from silently behaving like Panel 015.
